package com.example.pocJop.Repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.example.pocJop.Models.CapaciteArret;

public interface CapaciteArretRepository extends JpaRepository<CapaciteArret, Long> {
    
}
